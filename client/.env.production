# Production API (Railway). Used when building the client (Vercel, local vite build, CI).
# Railway monolith also works with relative /api if you omit this.
VITE_API_URL=https://popscore-production.up.railway.app
