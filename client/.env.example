# Production API base (no trailing slash). Repo ships .env.production for Railway.
# Local dev: leave unset (Vite proxies /api → localhost:5000).
# VITE_API_URL=https://popscore-production.up.railway.app
